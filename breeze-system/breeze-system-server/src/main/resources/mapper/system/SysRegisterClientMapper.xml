<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.breeze.cloud.system.mapper.SysRegisterClientMapper">

    <select id="listPage"
            resultType="com.breeze.cloud.system.domain.SysRegisteredClient">
        SELECT
            id,
            client_id,
            client_id_issued_at,
            client_secret,
            client_secret_expires_at,
            client_name,
            client_authentication_methods,
            authorization_grant_types,
            redirect_uris,
            scopes,
            client_settings,
            token_settings
        FROM
        sys_registered_client
        <where>
            <if test="registerClientQuery.clientName != null and registerClientQuery.clientName != ''">
                AND client_name LIKE CONCAT( '%', #{registerClientQuery.clientName} ,'%')
            </if>
        </where>
    </select>

    <select id="getRegisteredClientBy"
            resultType="com.breeze.cloud.system.domain.SysRegisteredClient">
        SELECT
        id,
        client_id,
        client_id_issued_at,
        client_secret,
        client_secret_expires_at,
        client_name,
        client_authentication_methods,
        authorization_grant_types,
        redirect_uris,
        scopes,
        client_settings,
        token_settings
        FROM
        sys_registered_client
        <where>
            <if test="registeredClient.id != null and registeredClient.id != ''">
                AND id = #{registeredClient.id}
            </if>
            <if test="registeredClient.clientId != null and registeredClient.clientId != ''">
                AND client_id = #{registeredClient.clientId}
            </if>
        </where>
    </select>

</mapper>
